[pytest]
# La cartella del progetto va nel percorso di import: senza, "pytest" lanciato
# da solo (come fa la CI, e come indica il README) non trova il pacchetto `app`.
# Funzionava solo "python -m pytest", che la aggiunge da sé.
pythonpath = .
testpaths = tests
